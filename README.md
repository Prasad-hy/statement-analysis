# statement-analysis
This project leverages Natural Language Processing (NLP) and Machine Learning (ML) techniques to process unstructured language and convert it into structured, interpretable results. Core components include text preprocessing (tokenization, stop-word removal, stemming, and lemmatization), feature extraction using methods like TF-IDF or word em
